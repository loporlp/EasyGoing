npm install expo-router
npx expo install firebase
npx expo install expo-auth-session
npm install @react-navigation/native
npm install @react-navigation/stack
npm install react-native-flags
npm install react-native-flags

npm install react-native-maps
npm install react-native-get-random-values
npm install react-native-google-places-autocomplete
npx expo install react-native-screens react-native-safe-area-context
npm install react-native-calendar-picker
npm install react-native-vector-icons@latest
npm install react-native-elements

# For the Routes
npm install react-native-maps axios
npm install polyline

# how to start the project
npx expo start -c # starts project -c clears cache, press a once its started for android

#if you have missing dependecies run
npm install

# if you have some kind of error like "unable to resolve" you may have a dependecy mismatch, run this to correct

#UNIX COMMANDS
rm -rf node_modules package-lock.json
npm install
# For Windows just delete the node_modules folder and package-lock.json by hand then run
npm install




#Doing this will change package.json and if you update all our dependecies everyon
# will have to run npm install to keep up
npm install -g npm-check-updates #install update checker
ncu #check for updates
ncu -u #update dependecies 
npm install #install the updates

